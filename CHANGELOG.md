# 변경 이력

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르고,
버전은 [유의적 버전](https://semver.org/lang/ko/)을 따른다.

## [Unreleased]

## [0.2.7] — 2026-07-31

### 수정 — 입력 칸이 뒤틀려 있던 문제

두 칸짜리 행의 **왼쪽 칸이 오른쪽보다 8px 위에** 있었다. `.field:first-of-type`이
`.row` 안에서 왼쪽 칸에만 걸려 위쪽 여백이 달라졌기 때문이다. 여백을 각 칸이 아니라
컨테이너가 갖게 바꿨다. 컨트롤 높이도 숫자 입력 36px / 셀렉트 40px로 갈려 있던 것을
38px로 통일했다.

실제 크로미움으로 좌표를 재서 확인했다: 고치기 전 6개 행 전부 라벨·입력 **8.0px 어긋남**,
높이 36/40 두 종류. 고친 뒤 **전부 0.0px**, 높이 15개 모두 38px.

라벨이 길어 두 줄로 접히는 경우까지 맞추려고 `subgrid`로 라벨 트랙을 공유시켰다
(미지원 환경에서는 `min-height`가 한 줄 케이스를 잡는다).

### 수정 — 로딩이 멈춘 것처럼 보이던 문제

Python 쪽은 느리지 않았다. 카탈로그 48ms, 페이지 생성 0.1ms다. 느린 것은
**브리지를 15초 기다리는 동안 화면이 완전히 비어 있는 것**이었다 — 레일의 숫자 칸이
전부 스크립트로 채워지고 있어서, 브리지가 죽으면 15초 동안 빈 폼을 보게 된다.
그건 느린 창이 아니라 고장난 창으로 읽힌다.

- **기본값을 마크업에 박았다.** 첫 바이트의 JSON보다 먼저 그려진다. JS를 완전히 끈
  브라우저에서 숫자 입력 11개가 전부 채워지는 것을 확인했다
- 스크립트에 있던 같은 값의 **두 번째 사본을 제거**했다 — 어긋날 수 있는 복제였다
- **브리지 대기를 15초 → 5초**로(Python 쪽 구조는 18초 → 6초). 살아 있는 브리지는
  밀리초 단위로 답한다. 긴 상한은 성공을 빠르게 하지 못하고 실패를 늦게 인정할 뿐이다

### 수정 — 말투

사용자 지적: *"전환 중 잠시 기다려라, 무슨 반말로 하냐?"*

앱 전체가 명령형이었다. 사용자에게 보이는 문구 **30건을 존댓말로** 바꿨다
(`gui.py` 11 · `cli.py` 7 · `lab.py` 4 · `measured.py` 3 · 그 외 5).
한 군데만 고치면 다음 화면에서 또 만나게 된다. 테스트가 두 페이지 전체를 훑어
명령형이 다시 들어오는 것을 막는다.

## [0.2.6] — 2026-07-31

### 수정 — id 하나가 겹쳐서 화면 전체가 깨져 있었다

사용자 지적: *"항목별 UI 전부 깨지고 정상작동 안 함. LLM 선택하는 곳에 분류도 깨져 있고."*

**`<main id="model">`과 `<select id="model">`이 같은 id를 쓰고 있었다.** 3탭 개편(0.2.4에
포함) 때 모델 성능 화면 컨테이너에 `model`을 붙였는데, 그 id는 이미 LLM 드롭다운이
쓰고 있었다.

그래서 `getElementById("model")`이 셀렉트가 아니라 `<main>`을 돌려줬다:

- 부트 코드가 `<optgroup>` 6개와 모델 32개를 **화면 컨테이너에 쏟아부었다** — 옵션 그룹이
  레이아웃 위에 텍스트로 흩뿌려졌다
- **LLM 드롭다운은 끝까지 비어 있었다** — "분류가 깨져 보인다"의 정체다
- `byId("mb-model").value = byId("model").value`가 `<main>`의 `.value`를 읽어 undefined

아무것도 예외를 던지지 않는다. **중복 id는 조용히 실패한다.** 그래서 "전부 깨졌다"로
보였고, 그 표현이 정확했다. 0.2.4와 0.2.5 두 릴리스가 이 상태로 나갔다.

- 화면 컨테이너를 `screen-model` / `screen-size` / `screen-lab`으로 바꿨다.
  셀렉트 id는 라벨(`<label for="model">`)과 저장된 상태가 물려 있어 건드리지 않았다
- **중복 id 검사를 테스트로 고정했다** — 서버·데스크톱 양쪽 페이지 전체를 훑는다.
  이 종류의 버그는 다시 만들면 또 조용히 나간다
- 모델 분류 라벨을 캡션으로 바꿨다: `sub-2B` → **2B 미만**, `70B+` → **70B 이상**.
  카탈로그 키를 그대로 드롭다운에 노출하고 있었다

검증 방식의 문제이기도 했다. 앞선 릴리스에서 DOM 검사를 돌려 통과시켰지만 **내가 바꾼
화면만 봤고 내가 깨뜨린 화면은 보지 않았다.** 이번 검사는 세 탭을 전부 눌러 각각 정확히
한 화면만 뜨는지, 셀렉트가 전부 채워지는지, JS 오류가 0건인지까지 본다.

## [0.2.5] — 2026-07-31

### 수정 — 창이 안 뜨면 앱이 스스로 고친다

사용자 지적: *"데스크톱 브리지가 15초 안에 준비되지 않았다면서 응답없음 나온다."*

0.2.3에서 패키징을 고쳤고 그 수정 자체는 맞았다(리눅스에서 같은 옵션으로 실제 빌드해
확인했다 — `webview.__file__`이 zip 밖으로 나가고 glob이 `api.js`를 찾는다). 그런데
브리지가 죽는 원인은 **전부 이 프로그램 바깥에 있다** — WebView2 런타임 부재, 주입
스크립트를 걷어내는 보안 소프트웨어, 수정 이전 버전 설치본. 그리고 전부 똑같이 보인다:
창은 그려지고 아무것도 안 된다.

그때 "터미널에서 `svrspec gui`를 실행해라"라고 말하는 것은 수정이 아니라 **떠넘기기**다.

- **자동 전환.** 페이지가 18초 안에 Python에 도달했다고 알리지 않으면, 앱이 스스로
  루프백에 서버를 띄우고 그 창을 거기로 다시 띄운다. 같은 페이지가 브리지 없이 돈다.
  포트는 임의 할당이고 127.0.0.1에만 묶이므로 외부에서 닿지 않는다
- **`svrspec-cli selfcheck`** — 이 설치본이 창을 띄울 수 있는지 설치본 안에서 검사한다.
  파일 목록을 읽는 것과 pywebview가 그 파일을 *찾는지*는 다른 질문이다: 모듈이
  library.zip 안에 남으면 경로가 아카이브를 가리켜 아무것도 매치되지 않는다.
  빌드 시점 파일 검사는 그 경우를 통과시킨다 — 실제로 통과시켰다
- **CI가 이제 그 검사를 돌린다.** 이전 스모크 테스트는 "12초 뒤에도 프로세스가 살아 있나"만
  봤고, 사용자가 겪은 증상은 그 검사를 그대로 통과한다

### 수정 — 탭 이름이 화면 내용과 달랐다

사용자 지적: *"적용사례 관제 알람은 왜 있는 거냐, 요청 안 했는데."*

맞는 지적이었다. 그 화면은 CPU·DIMM을 조립해 부하를 거는 **가상 랩**이고, 관제 알람은
거기 들어 있는 네 가지 부하 프로파일 중 하나다. 프로파일 하나의 이름을 화면 전체의
이름으로 붙여 놓아 알람 전용 기능처럼 보였다. **부하 테스트**로 고쳤다 — 기능은 그대로다.

### 추가 — 시뮬레이터가 자기 근거를 들고 있다

사용자 지적: *"해당 부분 로그로 남길 수가 없는데... 모든 스펙과 정보는 시뮬레이터에서
제공해줘야 하는 거 아냐? 로그 따서 진행할 거면 안 하지."*

맞는 지적이다. 실측 로그를 받아오라는 요구는 **하드웨어를 이미 가진 사람에게만** 가능하다.
이 도구의 전제는 그것이 아직 없다는 것이다 — 그래서 산정을 하는 것이다.

- **출처 없는 행 0건.** 33건이 남아 있었고 전부 메모리 행이었다(DDR4 등급과 2DPC 감속).
  전부 공개된 플랫폼 데이터라 모르는 값이 아니라 이 카탈로그의 구멍이었다.
  DDR5 4800→4400(2DPC), 5600→4400, DDR4 3200→2933, DDR4-2666 유지를 각각 벤더 문서와
  공개 자료로 근거화했다
- **MRDIMM 행을 걷어냈다.** 어떤 CPU도 쓰지 않아 도달할 수 없는데 근거도 없었다 —
  쓰이지 않는 추정치를 카탈로그에 두는 것은 신뢰만 깎는다
- **"이 숫자들의 근거" 패널.** 예측에 쓰인 계수 전부를 값·신뢰도·출처 링크와 함께 화면에
  띄운다. 손에 없는 서버를 산정하는 도구는 **그 자리에서 감사 가능해야** 한다.
  읽는 사람이 실물과 대조할 방법이 없기 때문이다
- 화면이 "납품 문서로 확정하기 전에 벤더 데이터시트로 대조해야 한다"고 말하던 것을 없앴다.
  숙제를 내주는 것은 답을 미루는 것이다

## [0.2.4] — 2026-07-31

### 수정 — 계산은 하는데 화면이 그리지 않던 값들

앞선 작업에서 payload에 넣은 필드가 화면에 나오지 않았다. **필드를 추가한 것과
화면이 그것을 보여주는 것은 별개**인데 전자만 하고 끝냈다.

- **OS를 고를 수 있다.** 엔진·payload·렌더러는 OS 프로파일을 지원했지만 **고를 컨트롤이
  없어 항상 기본값으로만 측정**됐다. "OS 종류별로 측정 가능"이 사실상 안 되던 상태다.
  같은 8B/Q4_K_M/8GB×2에서 최소 필요 RAM이 Linux 컨테이너 5.15GB ↔ Windows 데스크톱
  8.55GB로 갈린다 — 고를 수 없으면 이 차이는 존재하지 않는 것과 같다
- **지표에 "필요 메모리(RAM)"를 추가**했다. 격자 지점마다 RAM이 20배 움직이는데 표에서
  볼 수가 없었다
- **못 올리는 운용점을 ✕로 표시**한다. 어느 지표를 보고 있든 표시되고, 색만으로 뜻을
  나르지 않도록 마크를 함께 둔다
- OS 가정과 **초과 시 결과**(스왑이냐 OOM kill이냐)를 수치 옆에 렌더한다
- `catalog` payload가 `os_profiles`/`os_default`를 싣는다

검증 방식도 바꿨다. 화면 검증을 "문자열이 HTML에 있는가"로만 하면 이 종류의 구멍을
못 잡는다 — 문자열은 전부 있었다. jsdom으로 실제 서버에 붙여 **그려진 DOM**을 검사했고,
✕ 표시된 6칸이 payload의 `fits=false` 6행과 일치하는 것까지 확인했다.

### 수정 — RAM이 조건과 무관하게 고정으로 보이던 문제

사용자 지적: *"8B 모델이면 7GB로 고정되어 있음. 변동성이 없는데? 실제 추론 쪽 작업하면
변경이 될 수도 있을 텐데."*

맞는 지적이었다. `size_memory`는 실제로 슬롯·컨텍스트에 따라 변하는데, **모델 성능 화면이
한 지점에서만 계산해 단일 값으로 보고**하고 있었다. 격자는 배치 1~32, 컨텍스트 512~16384을
훑는데 화면은 숫자 하나였다.

- **격자의 각 지점이 자기 RAM을 갖는다.** llama.cpp는 슬롯마다 컨텍스트 전체를 미리 잡으므로
  배치 32·16k 컨텍스트는 배치 1·512의 **20배**를 요구한다 — 8B 기준 7.56GB에서 151.30GB다.
  요약 하나만 보고 서버를 산정한 사람은 표의 오른쪽 아래가 안 올라간다는 것을 나중에 발견하게 된다
- **장착량으로 안 되는 지점을 표시한다**(`fits`). 64GB 보드에서 어느 칸이 못 도는지가 그 칸에서 보인다

### 수정 — 앞선 릴리스에서 남겨 둔 리스크 셋

- **격자 축을 고를 수 있다.** 축이 서버에 고정돼 있어 요청을 무시했다. 400점 스윕을 막으려던
  것이고 그 걱정은 타당하지만, 화면의 존재 이유가 **자기 운영점**을 보는 것인데
  배치 48·컨텍스트 64k 같은 실제 구성을 볼 수 없었다. 이제 재정의를 받되 값은 clamp하고
  축 길이를 12점으로 제한한다 — 열어주되 무제한은 아니다
- **컨테이너의 실패 방식을 모델에 넣었다.** cgroup 상한 초과는 스왑이 아니라 **OOM kill**이고,
  이 비대칭을 여유계수 하나로는 표현할 수 없다. `hard_limit` 플래그와 함께
  "메모리를 넘기면 OOM kill로 프로세스가 죽는다"는 문장이 산정에 따라붙는다.
  컨테이너 여유계수를 1.35 → **1.6으로 올렸다** — 상주량이 가장 적다고 여유를 가장 적게 주면
  거래를 거꾸로 하는 것이다. 이제 상주는 가장 가볍고 여유는 가장 크다
- **OS 상주량을 실측으로 대체할 수 있다.** 계수를 `calibrate`로 승격하듯,
  `measured_os_profile(runtime_gb, source, base=...)`로 실제 서버에서 잰 값을 넣는다.
  **출처가 없으면 거부한다** — 어느 기계에서 쟀는지 없으면 실측이라 할 수 없다.
  여유계수와 `hard_limit`은 기저에서 상속한다: 메모리 측정값이 "넘치면 죽는지"를
  알려주지는 못하기 때문이다

### 추가 — OS별 메모리 프로파일

사용자 지적: *"OS별로도 측정 수치가 다를 것 같은데 OS 종류별로 측정도 가능하게 만들어라."*

`RUNTIME_OS_GB = 2.5` 상수 하나로 모든 OS를 뭉뚱그리고 있었다.

- **`OS_PROFILES` 4종** — Linux 컨테이너(0.6GB) · Linux 서버(1.0GB) · Windows Server(2.5GB) ·
  Windows 데스크톱(4.0GB). 상주량과 여유계수를 각각 갖는다
- `size_memory(..., os_name=...)`와 `bench_model(..., os_name=...)`로 고른다.
  **인자를 안 주면 기존 동작 그대로**라 모든 기존 호출자가 영향받지 않는다
- 기본값은 Windows Server다 — 패키지 앱이 설치되는 곳이고, 서버 프로파일 중 가장 비싸다.
  가장 싼 값을 기본으로 두면 생각 없이 지나간 산정이 조용히 과소 산정된다
- **처리량은 OS와 무관하다**는 것도 테스트로 고정했다 — OS는 남는 메모리를 바꾸지 실리콘을 바꾸지 않는다
- 이 수치들은 추정이다. 이 프로젝트는 Windows Server 유휴 상주량을 측정한 적이 없고,
  각 프로파일이 `note`로 그 사실을 밝힌다

### 추가 — 모델 성능이 기본 화면이 됐다

사용자 지적: *"부하 테스트 기준을 알람 말고 추론·연산·학습으로 해라. 알람 테스트는 서브로
빼고, 실제 그 모델을 대입했을 때의 성능을 보고 싶은 거다. 너무 알람 테스트로만 들어가 있다."*

맞는 지적이었다. 이 도구는 "관제 알람 파이프라인"이라는 **하나의 응용**에 갇혀 있었고,
그 앞의 더 기본적인 질문 — **"이 서버에 이 모델을 올리면 몇 tok/s 나오나"** — 에 직접
답하는 화면이 없었다. 알람 판정은 프롬프트 캐시·스톰 도착·큐·SLA라는 네 가지 가정을 거친
결과이고, 그 중 하나만 바꿔도 판정이 뒤집히는데 **기계는 그대로다.**

- **`svrspec/modelbench.py`** — 워크로드 모델이 하나도 없는 벤치. 도착 과정도 SLA도 큐도
  프롬프트 캐시도 없다. 배치 × 컨텍스트 격자, 동시 사용자 스윕, 병목 분해, 학습 판정뿐이다
- **Generation tok/s와 TTFT** — 사람이 실제로 체감하는 두 숫자를 격자에 이름으로 넣었다.
  화면이 "Generation tok/s"라 부르는데 엔진이 `decode_tps_single`이라 하면 읽는 사람이
  매번 번역해야 한다. 한국어·원어 병기로 라벨링했다
- **배치의 대가가 보인다.** 배치를 키우면 총 처리량은 오르는데 한 사용자의 체감 속도는
  떨어진다 — 7B/Gold 6426Y에서 배치 1→32에 총량 45→156 tok/s, 체감 45.1→4.9 tok/s.
  평평한 선 하나로 그리면 CPU 추론에서 가장 중요한 거래가 사라진다
- **컨텍스트의 대가도 보인다.** ctx 512→16384에서 TTFT 3.3초→105.0초, 생성 47.1→39.4 tok/s
- **학습·파인튜닝 판정** — full/LoRA/QLoRA. 대개 답은 "안 된다"이고, **메모리가 아니라
  시간 때문**이라는 것이 이 축의 값어치다. 7B는 셋 다 메모리가 들어가는데(117/18.6/7.9GB)
  1 epoch이 109/73/95시간이라 반복 실험이 불가능하다. 근거·가정·GPU 비교(A100 1장 대비
  약 52배)를 함께 낸다. **학습 계수는 카탈로그에 근거가 없는 추정이고 그 사실을 매 판정마다 밝힌다**
- GUI를 **[모델 성능][자원][적용 사례: 관제 알람]** 3탭으로 재편했다. 기본은 모델 성능이고,
  **기존 화면은 하나도 사라지지 않았다**(테스트가 11개 마커로 강제)
- `desktop.Api.modelbench` 연결

## [0.2.3] — 2026-07-31

### 수정 — 브리지 JS가 빌드에 아예 복사되지 않던 문제 (0.2.2 보완)

0.2.2에서 `webview`를 zip 밖으로 뺐지만 그것만으로는 부족했다. **cx_Freeze는 패키지의
모듈을 복사하지 `js/*.js` 같은 데이터 파일을 가져가지 않는다.** 카탈로그 JSON을
`include_files`로 명시하는 것과 같은 이유로 pywebview의 JS도 명시해야 한다.

- `webview/js` 를 `include_files` 에 추가했다
- **JS가 없으면 빌드를 거부한다.** 이 결함은 조용히 실패하는 성질이라 — 창은 열리고
  페이지도 그려지고 다만 영영 채워지지 않는다 — 산출물이 나온 뒤에 알아채면 늦다.
  `api.js`·`finish.js`가 없으면 `SystemExit`으로 빌드를 멈춘다

### 변경 — 업데이트가 재설치가 아니라 업데이트처럼 동작한다

이전에도 버튼 한 번으로 받아서 설치까지 갔지만, 설치 마법사가 뜨고 라이선스와
설치 경로를 다시 확인해야 했고 끝나면 앱을 손으로 다시 켜야 했다.

- **마법사 없이 조용히 설치한다.** `/SILENT /SUPPRESSMSGBOXES /NORESTART
  /CLOSEAPPLICATIONS /RESTARTAPPLICATIONS`로 실행한다. 사용자는 버튼을 누르며
  이미 동의했고, 한 번 고른 설치 경로를 다시 고르게 하는 것은 동의가 아니라 마찰이다
- **창이 닫혔다가 새 버전으로 다시 열린다.** Restart Manager가 실행 중인 앱을 닫고
  파일을 교체한 뒤 다시 띄운다. `.iss`에 `CloseApplications`·`RestartApplications`를
  켰다 — 이게 없으면 실행 중인 .exe를 덮어쓸 수 없어 설치가 실패하거나 재부팅으로 미뤄진다
- **앱 업데이트로 Windows를 재부팅하지 않는다**(`/NORESTART`, `AlwaysRestart=no`)
- 마법사 경로는 `launch_installer(path, silent=False)`로 남겨 뒀다 — 조용한 설치가
  실패했을 때 원인을 보려면 필요하다

## [0.2.2] — 2026-07-31

### 수정 — 데스크톱 앱의 브리지가 아예 뜨지 않던 근본 원인

0.2.0에서 "15초 안에 준비되지 않았다"는 오류를 띄우게 만든 것은 증상 처리였다.
**브리지가 왜 안 뜨는지가 진짜 문제였고, 그건 패키징에 있었다.**

- **cx_Freeze가 `webview` 패키지를 library.zip에 넣고 있었다.** pywebview는 브리지를
  `webview/js/**/*.js`를 **glob으로 디스크에서 찾아** 주입한다(`webview.util.load_js_files`).
  zip 안에서는 이 glob이 **하나도 못 찾는다.** 그래서 `api.js`가 실행되지 않고
  `window.pywebview`가 만들어지지 않으며 `pywebviewready`도 영영 발화하지 않는다.
  `zip_exclude_packages`에 `webview`를 추가했다
- **조용히 실패한다는 것이 이 결함의 성질이다.** import는 성공하고 창도 열린다.
  다만 드롭다운이 끝내 채워지지 않고, 값도 비어 있고, 업데이트 버튼도 안 뜬다 —
  부트 체인이 브리지에서 멈춰 그 뒤가 전혀 실행되지 않기 때문이다
- **릴리스 스모크 테스트가 이걸 잡지 못했다.** "12초 뒤에도 프로세스가 살아 있나"만
  봤는데, 브리지가 죽은 빌드도 그 검사는 통과한다. 이제 `webview/js`의 실제 파일
  존재와 `api.js`·`finish.js`를 확인한다. 저장소 테스트로도 고정했다

## [0.2.1] — 2026-07-31

### 수정 — 부하 테스트 화면의 주의사항이 두 번 나오던 문제

- **같은 주의사항이 그대로 두 번 찍혔다.** 화면이 `profile.notes`와 `result.notes`를
  이어서 그리는데 실행 결과가 프로파일 주의사항을 복사해 담고 있었다. 작은 글씨 11줄 중
  5줄이 중복이었고, 그렇게 되면 아무도 끝까지 읽지 않는다.
  이제 프로파일은 부하에 대한 주의를, 실행 결과는 실행에 대한 주의만 담는다
- **문장을 줄였다.** 한 줄에 한 가지만 말한다 —
  "도착 시각은 ramp 부하율 곡선에서 다시 뽑았다 — 실측 시간대 분포가 아니다. 알람 내용·
  심각도·상관 구조는 실측 기반 생성기 값 그대로다" → "도착 시각은 ramp 곡선에서 뽑았다 —
  실측 시간대 분포가 아니다". 이유는 모듈 docstring에 있고 화면에는 사실만 남긴다.
  전체 11줄 → 7줄

### 수정 — 메모리 카탈로그에 8GB·16GB RDIMM 추가

- **32GB와 64GB만 실려 있어서 조언이 왜곡됐다.** 대역폭은 채널을 채워야 나오는데
  8채널 보드를 채우는 최소 구성이 8×32GB=256GB로 계산됐고, 그래서 도구가 "필요한 것의
  네 배를 사라"고 말하고 있었다. 8GB·16GB RDIMM은 실재하는 표준 서버 SKU이고,
  **8×8GB로 채워도 대역폭과 토큰 생성 속도가 8×64GB와 완전히 같다** — 다른 것은 용량뿐이다.
  DIMM 용량은 1DPC에서 클럭을 바꾸지 않는다(속도는 DDR 등급과 채널당 장수의 속성)
- 그래서 채널 미충족 대안이 달라진다. "2×64GB → 8×32GB로 **올려라**(256GB)"가 아니라
  "같은 128GB를 **8×16GB로 나눠라**"가 된다. 7B 기준 RAM 비용이 $1,200 → $440으로 내려간다
- 메모리 카탈로그 35 → 69행. 랭크 수(1Rx8 대 2Rx8)는 모델링하지 않는다 —
  채널당 2장에서는 영향이 있을 수 있으므로 그 구성의 수치는 표의 나머지와 같은 1차 근사다

## [0.2.0] — 2026-07-31

### 수정 — 데스크톱 앱이 기동하다 멈추던 문제

- **창이 "카탈로그를 불러오는 중…"에서 영구 정지할 수 있었다.** `bridgeReady()`가
  `pywebviewready` 이벤트만 기다렸는데, pywebview가 이 스크립트보다 **먼저** 이벤트를 발화하면
  리스너가 영영 불리지 않는다. 프로미스가 settle되지 않으므로 `.catch()`에도 닿지 않아
  오류 한 줄 없이 멈춘 화면만 남았다.
  이제 이벤트와 **폴링을 병행**하고(이벤트를 놓쳐도 객체가 생기면 즉시 진행),
  15초 안에 준비되지 않으면 원인과 대안(WebView2 확인, `svrspec gui` 서버 방식)을 띄운다.
  실제 이벤트 루프에서 세 시나리오로 검증했다 — 정상 수신 122ms, **이벤트 놓침 201ms 복구**,
  브리지 부재 15초 후 명시적 실패
- 기동 실패 시 "불러오는 중" 자리표시자를 치운다. 실패했는데 계속 로딩 중이라고 말하지 않는다

### 추가 — 가상 하드웨어 랩

CPU·소켓·DIMM 용량과 개수를 직접 골라 가상 서버를 조립하고, 그것에 부하를 걸어보는 랩.
브라우저 GUI가 주 화면이다.

- **채널 미충족을 모델링한다.** `perf.effective_bandwidth`와 `predict_throughput`에
  선택 인자 `channels_populated`를 추가했다(기본값은 전 채널 장착이라 기존 호출자는 동작 불변).
  **8채널 보드에 DIMM 2장을 꽂으면 대역폭이 정확히 4분의 1**이 되고, decode는 대역폭
  바운드이므로 토큰 생성 속도가 그대로 4분의 1이 된다. 용량은 멀쩡해 보이는데 성능이 무너지는
  실무에서 가장 흔한 구매 실수인데, **지금까지는 그 구성을 표현할 수조차 없어 경고도 못 했다.**
  예측의 `warnings`에 손실 %와 "전 채널에 나눠 꽂으면 N배 빠르다"가 함께 나온다
- 채널이 선형으로 기여한다고 본다. 실제 컨트롤러는 어중간한 장착 수에서 인터리빙 효율도
  잃지만 그건 모델링하지 않는다 — 미충족 구성의 수치는 낙관적 상한이지 약속이 아니다
- **`svrspec lab`** — CPU·소켓·DIMM 용량·개수를 직접 골라 머신을 조립하고 검증한다.
  채널 미충족·RAM 부족·CPU 최대 장착량 초과·소켓 초과·DPC 클럭 하락·불균등 분배를 잡아내고,
  **문제만이 아니라 대안을 함께 낸다**("같은 128GB를 8×16GB로 전 채널에 나눠 꽂으면
  대역폭이 4.0배로 돌아온다" — 0.2.1에서 카탈로그에 소용량 모듈을 넣기 전에는
  "8×32GB로 올려라"였다).
  JSON으로 저장·불러오기가 되고, 조립이 error를 담으면 종료코드 1
- **`svrspec bench`** — 조립한 머신에 부하를 걸어 실행하고 프레임 시계열을 낸다.
  프로파일 4종: 실측 재생 · 램프(무너지는 지점 탐색) · 스파이크 · 소크.
  `--live`는 터미널에 스파크라인으로 재생하고, 램프는 **SLA를 처음 넘긴 지점**을
  부하율과 함께 찍어준다. 하루치 실행이 12~100ms라 스트리밍·백그라운드 스레드가 필요 없다 —
  서버가 즉시 완주시키고 브라우저가 프레임을 재생한다
- 램프가 채널 미충족의 대가를 숫자로 보여준다: 같은 CPU·같은 모델에서 8채널 완전 장착은
  5,000건/일까지 SLA를 유지하는데 2장만 꽂으면 **2,575건/일에서 무너진다**

### 변경

- **`timeline._rates`를 공개 `segment_rates`로 승격했다.** 이 프로젝트에서 "일을 바이트와
  FLOPs로 바꾸는 정의"는 하나여야 하는데, `hostsim`이 손으로 관리하는 복제본을 갖고 있었고
  랩이 세 번째를 만들 참이었다. 복제본은 어긋나고, 어긋나면 두 화면이 같은 순간에 대해
  다른 대역폭을 말한다. 이제 한 곳이고, 공식의 고유 항(`sweeps_s` 등)이 `timeline.py` 밖에
  나타나면 테스트가 잡는다

### 추가 — 돌려볼 수 있는 파이프라인

산정 결과를 표로만 보는 대신 **실제로 알람을 흘려볼 수 있게** 했다. 모델은 여전히 돌리지 않는다 —
서비스 시간은 해석 예측에서 계산하므로 이 머신에 부하가 걸리지 않는다.

- **`svrspec mock`** — 고객사 실측 알람량을 재현하는 목데이터 생성기(`mockdata.py`).
  `2026-06-01`은 정확히 **359건**, 6월 한 달은 정확히 **4,411건**으로 실측과 일치한다.
  장비·알람코드·심각도·한국어 메시지·사이트와 **상관 그룹**(스톰 대표 알람과 그 파생)을 갖춰
  3단계 상관분석·중복제거의 테스트 데이터가 된다. 시간대 분포·스톰 규모·심각도 비율이
  가정이라는 사실을 `notes`로 항상 함께 낸다
- **`svrspec serve`** — 알람 수신 → FIFO 큐 → 슬롯(prefill/decode) → Teams 싱크까지가 실제 코드
  경로로 도는 런타임(`pipeline.py`). 알람 한 건마다 도착·시작·첫토큰·생성완료·전달 시각을 기록하고,
  끝나면 p50/p95/스톰 소진/큐 깊이/처리 토큰을 낸다. `--speed`로 실시간 배속 재생,
  `--queue-limit`으로 백프레셔(상한 초과 시 새 알람을 버린다), `--trace`로 처리 로그
- 두 엔진이 같은 물리를 쓴다는 것을 테스트로 강제한다 — 같은 조건에서 `simulate()`와
  `run_pipeline()`의 p95 **상대오차 0**. 파이프라인은 `simulate.py`의 슬롯 공유 모델을
  재도출하지 않고 그대로 따른다
- Teams 전송은 `Sink` 프로토콜로 추상화했다. 기본 구현은 메모리 수집이고 실제 Adaptive Card
  형태의 카드를 만든다 — **여기서 HTTP를 치지 않는다.** 에어갭 환경이기도 하고, 드라이런이
  실 운영 채널을 울리면 안 되기 때문이다

### 추가 — 정적 요약에서 시계열 시뮬레이션으로

사용자 지적: *"리소스 사용량도 변화가 없잖아. 추정치로만 되어 있는 것 같은데, 실제로
시뮬레이터를 돌린다는 생각이 들 정도로 만들어야 한다."* 맞는 지적이었다. 리소스 칸 세 개가
사실상 같은 스칼라(`busy_fraction`) 하나를 복사해 쓰고 있었고, RAM은 고정 할당량이라 아예
움직이지 않았다. 세 그래프가 한 줄이었다.

- **`svrspec timeline`** — 하루 리소스 사용량을 위상별로 분해한 시계열. CPU 가동·대역폭·연산·
  KV 실사용·큐 깊이·도착/완료를 **각각 다른 계열**로 낸다. prefill은 벡터유닛을 태우고 DRAM은
  놀며 decode는 정반대이므로, 대역폭 그래프와 연산 그래프는 서로 반대로 움직인다. 한 줄로 겹쳐
  그리면 이 정보가 통째로 사라진다. 터미널 스파크라인 + 가장 바쁜 구간 표 + CSV
- **`svrspec capacity`** — 이 서버가 **어디서 무너지는지**. 부하 축 네 개(일 알람 건수 / 30초
  창 스톰 크기 / 알람당 프롬프트 토큰 / 알람당 생성 토큰)를 각각 램프시켜 SLA가 깨지는 무릎을
  이분 탐색으로 찾고, **무엇이 먼저 무너졌는지**(평상시 지연 / 스톰 소진 / RAM)를 함께 낸다.
  네 축을 함께 돌리면 가장 먼저 무너질 축이 나오고, 그것이 곧 다음에 돈을 써야 할 곳이다
- `svrspec/timeline.py` — 시뮬레이터가 내는 구간(모든 rate가 일정한 구간)을 물리량으로 환산.
  구간은 상태 변화마다 끊기므로 이 시계열은 표본이 아니라 **재구성**이다
- `svrspec/capacity.py` — 무릎 탐색. 기하급수 브래킷 + 이분 탐색이라 비용이 답의 로그다.
  가는 길에 평가한 모든 지점을 곡선으로 남긴다 — 주장이 아니라 실험으로 읽히게 하기 위해서다
- `SimTrace`가 위상 분해된 구간(`SimSegment`)과 도착/완료 시각을 기록한다. 이전에는
  (시각, 활성, 대기, 지속) 4-튜플뿐이라 어느 자원이 얼마나 쓰였는지 복원할 수 없었다
- **`svrspec calibrate`** — 실측 로그를 읽어 효율 계수를 실측으로 승격한다. `llama-bench`
  마크다운 표·`-o json`·`llama-server` 로그 세 포맷을 읽고, 예측 대비 오차를 먼저 보여준 뒤
  계수를 유도한다. **아무것도 실행하지 않는다.** 산정 대상 서버가 손에 없는 도구가 실측을 얻는
  정직한 방법은 남이 돌린 로그를 읽는 것뿐이다
- `svrspec/hostsim.py` — 모니터링 에이전트가 남겼을 법한 고정 주기 호스트 샘플(CPU·코어별
  부하·load average·RSS·대역폭·큐). 실제 호스트를 들여다보지 않고 시뮬레이션 산출물에서 유도한다.
  `timeline --host-csv`로 내보낸다
- `SOURCE_MEASUREMENT` 출처 등급 — 실제로 돌린 벤치마크 로그에서 유도한 값. 3자 스펙표보다
  위다(예측 대상 자체의 측정이므로). 다만 인용 가능한 공개 페이지가 아니라서 `source_url`이
  URL이 아니라 로그의 신원(파일 경로·티켓·런 라벨)을 담는다

### GUI

- **작업관리자가 계열 17개를 각각 그린다.** 타일 6개(CPU·메모리·대역폭·연산·동시 처리·큐)가
  각자 자기 스파크라인을 갖고, 큰 그래프는 평균과 피크를 두 선으로 겹쳐 그린다. RAM은 움직이는
  실사용과 고정 예약선 두 개다
- 상단 병목 한 줄 — "이 부하에서 이 서버는 메모리 대역폭 바운드다 — 작업시간의 64%. 그 작업시간은
  prefill 36% / decode 64%로 갈린다" + 채널을 살지 코어를 살지에 대한 조언
- **순간 포화를 붉게 칠하지 않는다.** 100%는 decode의 정의이지 경고가 아니다. 대신 큐가 쌓인
  구간만 그래프 바닥에 표시한다
- 과부하 지점 화면 신규 — 축 4개 표(현재/무릎/붕괴/여유/한계요인) + 선택 축의 곡선(SLA 가로선,
  무릎 마커, 통과·미달 색 구분). 비용이 커서 버튼으로만 돌고, 축·지표 전환은 재요청 없이 처리한다
- `_buckets()` 제거 — `Timeline`이 상위 호환이고, 두 벌로 갈라지면 두 화면이 서로 다른 숫자를
  말하게 된다
- 데스크톱 앱(`Api.capacity`)에도 과부하 분석을 연결했다. 네이티브 창이 Windows 배포의 주 경로인데
  "서버 모드로 실행하라"는 에어갭 운영자에게 답이 아니다

### 수정

- **과부하 시 무한 루프.** 부하가 넘치면 백로그가 자정을 넘겨 소진되는데, 창 밖으로 나간 구간을
  버킷에 배분하면서 폭이 0인 조각을 무한히 잘라내고 있었다. 이제 마지막 버킷에 실어 일의 총량을
  보존하고, `overran_s`로 "하루 안에 소화되지 않았다"를 명시한다
- 구간 포화 표시가 대역폭을 무조건 먼저 보던 문제. 한 구간 안에서 두 천장이 모두 닿으므로
  고정 순서로 검사하면 코드 순서를 보고하게 된다. 이제 **더 가깝게 붙은 쪽**을 보고한다
- **일 알람 축의 무릎이 허수로 나오던 문제.** 스톰 알람은 일일 예산에서 차감되므로(추가가 아니라)
  알람량을 스톰 총량 아래로 내리면 하루 전체가 스톰이 되어 평상시 트래픽이 사라진다. 그 구간까지
  내려가면서 "하루 359건을 여유롭게 처리하는 빌드의 무릎은 4건"이라는 산출물이 나왔다. 이제
  스톰 총량에서 탐색을 멈추고 그 이유를 명시한다
- **2소켓 로그에서 유도한 계수가 `실측`으로 표시되던 문제.** 소켓 간 확장 손실이 계수에 흡수되는데
  그 손실은 `dual_socket_efficiency`가 따로 빼므로 2소켓 예측에서 이중 차감된다. 자동으로
  실측유도로 낮춘다. 로그의 스레드 수가 대상 부품의 코어 수보다 크게 적을 때도 마찬가지다 —
  로그가 그 기계의 것인지 의심할 유일한 근거다
- 실측 금지 가드가 **문자열 언급**을 막고 있던 문제(`llama-bench`라는 낱말이 cli.py에 있으면
  실패). 가드가 막아야 하는 것은 프로세스 실행이므로 `subprocess`/`Popen`/`os.system` 계열을
  **패키지 전체**에서 검사하도록 바꿨다 — 더 정확하고 더 넓다

### 추가

- **작업관리자 화면** — Windows 작업관리자 성능 탭 형태로 다시 만들었다. 왼쪽에 선택 가능한
  리소스 타일(CPU·메모리·대역폭·동시 처리·큐)과 스파크라인, 오른쪽에 선택한 항목의 24시간
  그래프. 시뮬레이션 이벤트를 15분 버킷 96개로 접어 그리므로 30초 스톰도 스파이크로 남는다
- 메모리 구성 바 — 가중치 / KV / 컴퓨트 / OS / 여유를 Task Manager처럼 한 줄로
- CPU 카탈로그에 최신 두 세대 추가: **EPYC 9005 Turin 6종**(Zen 5, AVX-512 전폭, 12ch DDR5-6400),
  **Xeon 6 Granite Rapids 3종**(AMX, 8ch DDR5-6400/1DPC). 28종 → 37종

### 변경

- **개수별 리소스가 필요 슬롯을 직접 풀어서 낸다.** 이전에는 설정된 슬롯 수로 RAM을 계산해
  알람 개수와 무관하게 같은 값이 나왔다. 이제 개수마다 SLA를 지키는 최소 슬롯을 찾고 KV 캐시와
  RAM이 거기서 따라온다. 슬롯을 늘려도 개선이 멈추면 그 지점에서 멈춘다 — 총 처리량이 연산이나
  대역폭에 걸린 것이고 그때는 동시성이 아니라 더 빠른 CPU가 답이다
- 저작권자를 rokaproj로 정정 (LICENSE, pyproject, 설치 프로그램 메타데이터)
- DDR5-6400 메모리 4행의 출처를 확인으로 승격 — 2DPC 하락값(5200)이 인용 문서와 일치

### 수정

- **창을 줄이면 입력칸이 결과 위로 떨어져 나란히 보이지 않던 문제.** 단일 컬럼 전환점(1080px)이
  창 최소 크기(1024px)보다 높아서 창을 조금만 줄여도 레이아웃이 무너졌다. 전환점을 900px로
  내리고 창 최소를 1100px로 올려 항상 2컬럼을 유지한다. 창 기본 크기도 1600×1000으로 키웠다
- 스티키 레일 오프셋이 헤더 높이와 어긋나 두 열의 위쪽이 안 맞던 문제 — 양쪽이 같은
  `--header-h` 변수를 읽게 했다
- 입력 레일이 자체 스크롤을 갖는다. 긴 폼이 잘리거나 결과 열을 늘려 놓지 않는다

## [0.1.0] — 2026-07-30

첫 배포.

### 산정 엔진

- CPU 추론의 두 병목을 분리한 루프라인 모델 — 토큰 생성은 메모리 대역폭 바운드,
  프롬프트 처리는 연산 바운드(AVX2 / AVX-512 / AMX 벡터 폭)
- 알람 도착·스톰을 이산사건 큐로 시뮬레이션. 평상시 지연 SLA와 스톰 소진 목표를 분리 판정
- 판정은 예측 불확실도만큼 성능을 깎은 **불리한 추정** 기준
- RAM 산정(가중치 + KV 캐시 + 컴퓨트 버퍼 + OS)과 실제 GGUF 파일 대조 검증
- 최소 / 권장 / 여유 3단 스펙 산출

### 카탈로그

- 모델 32종 (0.5B~70B, MoE 2종). 전부 실제 HuggingFace `config.json`에서 수집
- CPU 28종 (Xeon SP 3·4·5세대, Xeon E-2400, EPYC Milan·Genoa·Siena).
  AMD 10종은 벤더 데이터시트, Intel 18종은 Wikipedia SKU 표 + PassMark
- 메모리 35종. 채널당 2 DIMM 속도 하락 반영
- 효율 계수 9종. 각 행이 실측 / 실측유도 / 추정 중 무엇인지 표시
- 모든 행에 출처가 필수이고, 미확인 행은 리포트에 별도로 나열된다
- PassMark CPU Mark로 전사된 스펙을 감사 (`catalog validate`)

### 화면

- 네이티브 데스크톱 창 (Edge WebView2, 서버·포트 없음)
- 브라우저 GUI (`svrspec gui`, 리눅스·에어갭 서버용)
- 토큰 전달 시뮬레이터 — 구간별 시간과 예측 속도 실시간 재생
- 작업관리자 — 하드웨어 고정, 알람 개수별 CPU·RAM·대역폭 사용량 비교
- 단일 파일 HTML 납품 리포트 + CSV + JSON
- 다크/라이트, 키보드 조작, 토큰 기반 디자인 시스템

### 배포

- Windows 설치 프로그램(`setup.exe`)과 MSI
- 에어갭 서버용 zip 번들 (`svrspec bundle`) — 표준 라이브러리만 쓰므로 `python3`만 있으면 실행
- GitHub 릴리스에서 업데이트 확인 및 설치 (SHA-256 검증 후 실행)
